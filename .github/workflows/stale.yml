# file: .github/workflows/stale.yml
# version: 1.1.0
# guid: 336b7a2d-c53a-4ef2-927e-3bdfd733c528

name: Handle Stale Issues

permissions:
  issues: write
  pull-requests: write

on:
  schedule:
    - cron: '0 3 * * 1'
  workflow_dispatch:

jobs:
  stale:
    runs-on: ubuntu-latest
    steps:
      - name: Mark and close stale issues and PRs
        uses: actions/stale@v10
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          days-before-stale: 90
          days-before-close: 14
          stale-issue-message: >-
            This issue has been automatically marked as stale because it has not had
            recent activity. It will be closed if no further activity occurs.
          stale-pr-message: >-
            This pull request has been automatically marked as stale due to inactivity.
            It will be closed if no further activity occurs.
          close-issue-message: >-
            Closing as stale. Feel free to reopen with additional context or updates.
          close-pr-message: >-
            Closing as stale. Please reopen if you plan to continue work here.
          exempt-issue-labels: 'pinned,security,dependencies'
          exempt-pr-labels: 'pinned,security,dependencies'
          operations-per-run: 200
