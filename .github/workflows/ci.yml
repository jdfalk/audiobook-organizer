# file: .github/workflows/ci.yml
# version: 2.3.5
# guid: f1a2b3c4-d5e6-f7a8-b9c0-d1e2f3a4b5c6

# CI should be using the upstream shared ci
name: Continuous Integration

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  schedule:
    - cron: '0 0 * * 0' # Weekly on Sunday
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write
  actions: write
  checks: write
  packages: write

jobs:
  ci:
    name: Run CI
    uses: jdfalk/ghcommon/.github/workflows/reusable-ci.yml@b904a08523f11927f2fa22c83d349bf471d73ef6 # main
    with:
      go-version: '1.25'
      node-version: '22'
      python-version: '3.13'
      rust-version: '1.76'
      coverage-threshold: '0'
      skip-protobuf: false
    secrets: inherit
