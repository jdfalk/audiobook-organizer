# file: .github/workflows/nightly-burndown.yml
# version: 1.3.0
name: Nightly burndown

on:
  schedule:
    - cron: "0 8 * * *"
  workflow_dispatch:
    inputs:
      mode:
        description: "dry-run | draft-only | full"
        required: false
        default: draft-only
        type: choice
        options:
          - dry-run
          - draft-only
          - full

jobs:
  burndown:
    uses: jdfalk/ghcommon/.github/workflows/reusable-burndown.yml@3f152bd9ef75361223627217cfd22d4800d5a9a2
    with:
      mode: ${{ inputs.mode || 'draft-only' }}
    secrets: inherit
